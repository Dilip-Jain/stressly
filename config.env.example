# Configuration template for environment variables

# API Configuration
BASE_URL=http://localhost:3000
API_TIMEOUT=30000

# Authentication (choose one: bearer, basic, apikey, or none)
AUTH_TYPE=bearer
AUTH_BEARER=your_bearer_token_here

# Basic Auth (for AUTH_TYPE=basic)
AUTH_USERNAME=your_username
AUTH_PASSWORD=your_password

# API Key Auth (for AUTH_TYPE=apikey)
API_KEY_HEADER=X-API-Key
API_KEY=your_api_key_here

# Test Configuration
SCENARIO=load
VERBOSE=true

# LoadImpact Integration (optional)
LOADIMPACT_PROJECT_ID=0
